# Issue #6 — Search: гибридный поиск с `as_of` и цитатами

**Labels:** stage1, backend

* API: `POST /search {query, as_of, filters}`.
* Гибрид: BM25 (опц.) + вектор + rerank.
* Ответы содержат цитаты (норма/пункт + координаты).
* Юнит-тесты «на дату».

DoD:
* p95 ≤ 800 мс (до 200k чанков).
* Все ответы содержат корректные цитаты; `as_of` работает.
